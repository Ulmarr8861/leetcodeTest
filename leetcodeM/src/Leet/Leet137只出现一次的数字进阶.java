package Leet;

public class Leet137只出现一次的数字进阶 {
    /**
     * 这里的话每个数字出现了 3 次，
     * 所以我们可以加起来乘以 3 然后减去之前所有的数字和。这样得到的差就是只出现过一次的那个数字的 2 倍。
     */
    public static int singleNumber(int[] nums) {
        return 1;
    }
}
